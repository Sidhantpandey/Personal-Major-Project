# ──────────────────────────────────────────────────────────────────────────────
# OmniCrops ML Inference Service — Python Dependencies
#
# Auth, DB, and user management → Node.js (MongoDB/Mongoose)
# This service: image upload → model inference → return prediction JSON
# ──────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi>=0.115.6
uvicorn[standard]>=0.32.1

# Request validation / settings
pydantic>=2.11.0
pydantic-settings>=2.6.1
python-dotenv>=1.0.1

# File uploads (multipart/form-data)
python-multipart>=0.0.19

# ML inference (OmniCrops SwinV2-B+FPN)
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
Pillow>=10.0.0

# Development / testing (optional)
pytest>=8.3.4
pytest-asyncio>=0.24.0
httpx>=0.28.1
